name: clients_dart_api
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

dependencies:
  args: ^2.6.0
  dotenv: ^4.2.0
  get_it: ^8.0.2
  mocktail: ^1.0.4
  postgres: ^2.6.1
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  http: ^1.1.0
  lints: ^4.0.0
  test: ^1.24.0
